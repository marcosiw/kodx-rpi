# Kodx RPI — Contexto para IA

API .NET responsável por baixar RPIs (Revista da Propriedade Industrial) do site do INPI, validar consistência, converter para txt, salvar no Blob Storage (Azure) e persistir publicações individualizadas em Postgres, servindo o sistema Kodx e outros serviços consumidores.

- Especificação completa: `specs/Especificações - Kodx RPI.md`
- Código-fonte: `src/` (solução em `Kodx.Rpi.slnx`), testes em `tests/`

Antes de iniciar qualquer trabalho nesta base, leia este arquivo. Ao tomar decisões relevantes de arquitetura, fechar dúvidas em aberto ou avançar de fase, atualize-o.

## Status atual

Fase 7 (extração de publicações) **concluída e mergeada em `main`** (PR #6) — cobre Marcas, Patentes e ProgramasComputador; DesenhosIndustriais ficou de fora (ver itens 11 e 12 do plano, e a seção "Extração de publicações individuais (fase 7)" abaixo pra decisões/causa raiz).

Fase 8 (endpoints privados do Kodx API) **implementada, testada e pronta pra PR** — branch `feat/grpc-endpoints`. Decisão do usuário: os endpoints são **gRPC**, não REST (ver seção "Endpoints privados via gRPC (fase 8)" abaixo pra arquitetura, decisões e um bug real de Kestrel encontrado só ao rodar de verdade, não pelos testes automatizados).

## Convenção de Blob Storage do legado (confirmada contra produção real na fase 6)

Descoberta inicialmente por leitura do código do `kodx-legacy` (fase 3), e **corrigida na fase 6** após comparar com os blobs reais já existentes no container de produção (ver decisão "Armazenamento no Azure Blob Storage" abaixo — a leitura do código sozinha levou a um formato de tag errado):

- **Container Azure**: `rpi`
- **Path do blob** (relativo ao container): `{edicao}/{nomeArquivo}` — ex: `2501/Patentes2501.pdf`, `2501/Patentes2501.txt` (mesmo nome de arquivo usado localmente, via `RpiFileNaming`)
- **Tags aplicadas em cada blob**: `edicao` (ex: `"2501"`), `rpi` (**nome do tipo**, ex: `"Patentes"`, `"ContratosTecnologia"` — não o valor numérico do enum, apesar do código do legado sugerir isso por escrito), `extensao` (`"pdf"` ou `"txt"`)
- **Arquivo de referência no legado**: `/mnt/e/projects/kodx-legacy/src/4.Dominio/Servicos/BackupRpiServico.cs` — métodos `ArquivarPdfs()`, `ArquivarTxts()`, `ObterTags()`; a classe que fala com o Azure é `Kodx.Producao.Infra.Azure/StorageBlob.cs`
- A spec pede explicitamente seguir "o formato já existente de segregação e tags", e isso foi confirmado batendo com os dados reais em produção, não só com o código-fonte.

## Plano de implementação (por fases, validadas uma a uma)

1. ~~Scaffolding do repositório (docs de IA, git, estrutura de pastas)~~ — **concluído**
2. ~~Esqueleto da aplicação .NET (camadas DDD, Swagger, auth por API key, logging estruturado, Docker, CI básico)~~ — **concluído** (mergeado em `main` via PR #1)
3. ~~Modelagem e migrations do banco Postgres (histórico de downloads/transformações, publicações em JSONB)~~ — **concluído** (mergeado em `main` via PR #2)
4. ~~Download de RPIs do site do INPI~~ — **concluído** (mergeado em `main` via PR #3)
5. ~~Conversão PDF → TXT~~ — **concluído** (mergeado em `main` via PR #4)
6. ~~Armazenamento no Azure Blob Storage~~ — **concluído** (mergeado em `main` via PR #5)
7. ~~Extração e persistência das publicações individuais~~ — **validada contra o legado, pronta pra PR** (branch `feat/extract-publications`; cobre Marcas, Patentes, ProgramasComputador — DesenhosIndustriais ficou de fora, ver item 11)
8. ~~Endpoints privados para o Kodx API (consulta de RPI, download de PDF, busca de publicações por edição)~~ — **implementado via gRPC, pronto pra PR** (branch `feat/grpc-endpoints`)
9. Scripts de cronjob (Ubuntu 24.04) + documentação
10. CI/CD completo (sec scan, testes, build, deploy em versão fechada)
11. Reescrever as regras de leitura de Desenhos Industriais (`RpiDesenhosIndustriaisRules.cs`) a partir do layout atual da RPI — o parser herdado do legado está desalinhado com o formato que a INPI usa hoje pra esse tipo (ver decisão na fase 7 abaixo); não é um ajuste pontual de termo, é reescrever do zero
12. Corrigir o "cabeçalho inchado" de Patentes/Marcas (`RpiPatentesRules.cs`/`RpiMarcasRules.cs`) — a validação cruzada da fase 7 confirmou que o problema é paridade com o legado (não uma regressão do port), mas o usuário quer corrigir mesmo assim; decisão explícita de tratar como dívida técnica a resolver, não só documentar. Junto de DesenhosIndustriais, este item entra na mesma leva de "acertar as regras de leitura contra o layout real da RPI" — ver decisão na fase 7 abaixo pra detalhes de causa raiz

## Decisões tomadas

- Repositório GitHub: `git@github.com:marcosiw/kodx-rpi.git` (branch padrão `main`).
- Docs para IA ficam em `ai/`, com `ai/context.md` cumprindo o papel de guia principal (equivalente a um CLAUDE.md).
- `specs/` é versionado junto ao código (fonte de verdade da especificação).
- Fluxo de branches: cada fase do plano é implementada em uma branch própria a partir de `main`; ao finalizar, o merge em `main` é feito via **squash** (histórico enxuto na main, detalhado preservado na branch de origem). O PR #1 (fase 2) saiu como merge commit normal por engano; o usuário restringiu o repositório no GitHub (Settings > Pull Requests) para permitir só squash merge dali em diante.
- Esqueleto da aplicação (.NET 10, target `net10.0`):
  - Solução `Kodx.Rpi.slnx` na raiz; projetos de camada em `src/` (`Kodx.Rpi.Domain`, `Kodx.Rpi.Application`, `Kodx.Rpi.Infrastructure`, `Kodx.Rpi.Api`), testes em `tests/` (`Kodx.Rpi.Application.Tests`, `Kodx.Rpi.Infrastructure.Tests`, xUnit). Referências seguem DDD: Domain ← Application ← Infrastructure/Api.
  - Logging estruturado com Serilog + `UseSerilogRequestLogging()` para log de entrada/saída de requests. Console usa formato diferente por ambiente: em `Development`, template padrão do Serilog (texto já renderizado, legível no terminal); fora de `Development`, `CompactJsonFormatter` (JSON/CLEF, pensado para agregação de logs). No CLEF, `@mt` guarda o template bruto (não substituído) por design — os valores reais vêm como campos próprios no JSON (`RequestMethod`, `StatusCode` etc.) e `@r` traz o valor já formatado só para tokens com format specifier (ex: `{Elapsed:0.0000}`); não é um bug, é assim que ferramentas de agregação (Seq etc.) esperam consumir.
  - Auth por API key: middleware próprio (`Kodx.Rpi.Api/Security/ApiKeyMiddleware.cs`), header configurável (default `X-Api-Key`), valor lido de config/env (`ApiKey__Value`, nunca commitado). Endpoints `/health`, `/swagger` e `/favicon.ico` isentos (o favicon é isento porque o navegador o busca automaticamente ao abrir qualquer página, o que gerava um 401 de ruído no log mesmo com a página principal respondendo certo).
  - Timeout default de 30s via `AddRequestTimeouts`/`UseRequestTimeouts` (built-in do ASP.NET Core), pode ser sobrescrito por endpoint com o atributo `[RequestTimeout]` quando necessário.
  - Swagger via Swashbuckle (`AddSwaggerGen`/`UseSwagger`/`UseSwaggerUI`), habilitado em Development. Optou-se por Swashbuckle em vez do `Microsoft.AspNetCore.OpenApi` nativo do template porque este último trouxe uma dependência transitiva (`Microsoft.OpenApi` 2.0.0) com vulnerabilidade conhecida (NU1903) na versão do pacote puxada pelo template.
  - Docker multi-stage (`Dockerfile` na raiz, `.dockerignore`), imagem final expõe porta 8080.
  - CI (`.github/workflows/ci.yml`): jobs `security` (dotnet list package --vulnerable) → `test` (dotnet test) → `build` → `deploy` (só dispara em tags `v*.*.*`). O job `deploy` builda e publica a imagem no **GitHub Container Registry** (`ghcr.io/marcosiw/kodx-rpi`, tags `<versão>` e `latest`), usando o `GITHUB_TOKEN` (permissão `packages: write`) — sem action de terceiro, só `docker login`/`build`/`push` via CLI. **O deploy real na instância Linode (pull da imagem + restart do serviço) ainda não está implementado**, depende de decidir acesso/credenciais em fase futura.
  - Gatilho do CI é só `push` (branches + tags `v*.*.*`); não usamos `pull_request` porque duplicava a execução do pipeline inteiro a cada push numa branch com PR aberto para `main` (checks continuam aparecendo no PR pelo mesmo commit mesmo assim).
  - direnv: `.envrc.example` versionado como template; `.envrc` real ignorado pelo git.
  - Config local na IDE (Visual Studio/Rider): **um único mecanismo de config (variáveis de ambiente)** em todos os contextos — shell (`.envrc`/direnv), Docker/produção (env do orquestrador) e IDE. Para a IDE, que não herda o shell do direnv ao ser aberta direto (ex: ícone do Windows), usamos `src/Kodx.Rpi.Api/Properties/launchSettings.json`, que o Visual Studio/Rider já leem nativamente para variáveis de ambiente ao rodar o profile — **esse arquivo saiu do git** (contém valores locais como `ApiKey__Value`) e um `launchSettings.json.example` (com valores vazios) é o template versionado, no mesmo padrão do `.envrc.example`.
    - Avaliamos e descartamos `dotnet user-secrets`: ele só carrega em `Development` a partir de um arquivo fora do repo (`~/.microsoft/usersecrets/<GUID>/secrets.json`), nunca é publicado/empacotado e **não existe dentro de containers Docker** — seria uma segunda fonte de verdade desconectada de Docker/produção, o que o usuário preferiu evitar.

- Schema Postgres (fase 3, EF Core 10 + Npgsql, `EFCore.NamingConventions` para snake_case):
  - **Referência**: existe um repositório legado (`/mnt/e/projects/kodx-legacy`, `Kodx.Producao`, .NET/EF Core 6) que já roda em produção. Ele **não persiste** a RPI em si (entidade `Rpi` é só em memória) nem tem histórico de tentativas de download/conversão — esse é o gap que a fase 3 preenche. A tabela `publicacoes_pi` do legado guarda publicações em colunas de texto flat (`cabecalho1-3`, `conteudo`, `numero`, `pagina`, `index_inicio/fim`, `rodape`, `orgao`), ligadas à RPI só por `caderno_id` (tipo) + `edicao`, sem FK real.
  - **`RpiTipo`** (`Kodx.Rpi.Domain/Rpis/RpiTipo.cs`): reaproveita exatamente os valores do enum do legado (`Comunicados=1 ... TopografiaCircuitos=8`).
  - **`rpi_editions`**: nova tabela (o legado nunca persistiu isso) — `id`, `edicao`, `tipo`, `data_publicacao`, `created_at`; índice único em `(edicao, tipo)`.
  - **`rpi_processing_attempts`**: nova tabela cobrindo o gap de auditoria — `id`, `rpi_edition_id` (FK), `stage` (enum `ProcessingStage`: `Download`, `ConvertToTxt`, `ExtractPublications`, `UploadBlob` — 4 etapas independentes, cada uma com sua própria tentativa/status), `status` (`Success`/`Failure`), `error_message`, `started_at`, `finished_at`.
  - **`publications`**: `id`, `rpi_edition_id` (FK), `numero` (coluna real, indexada — decisão explícita do usuário, mesmo a spec sugerindo que filtragem fina ficaria por conta de outros serviços), `payload` (jsonb, via `OwnsOne(...).ToJson()` do EF Core/Npgsql — contém `Cabecalho1-3`, `Conteudo`, `TodosNumeros`, `IndexInicio/Fim`, `Rodape`, `Pagina`, `Orgao`, equivalente ao conteúdo das colunas flat do legado, sem duplicar `Numero`), `created_at`.
  - **Postgres local/testes**: só `docker-compose.yml` (Postgres 16, container `kodx-rpi-db`), **sem Testcontainers** — decisão explícita do usuário. Testes de integração em `Kodx.Rpi.Infrastructure.Tests/Persistence/KodxRpiDbContextTests.cs` assumem esse Postgres rodando (`docker compose up -d`) e aplicam `Database.MigrateAsync()` no `InitializeAsync`.
  - CI: o job `test` ganhou um Postgres 16 como *service container* do GitHub Actions (não é Testcontainers, é o mecanismo nativo de `services:` do Actions) para os testes de integração rodarem no pipeline.
  - Connection string via `ConnectionStrings__Postgres` (env var), seguindo o mesmo mecanismo único já estabelecido (`.envrc`, `launchSettings.json`/`.example`, CI). Valor local de dev: `Host=localhost;Port=5432;Database=kodx_rpi;Username=postgres;Password=postgres` (bate com o `docker-compose.yml`).
  - Migration inicial (`InitialCreate`) gerada, aplicada com `dotnet ef database update` contra o Postgres real do `docker-compose.yml` e confirmada com `\dt` (3 tabelas criadas). Os 2 testes de integração (round-trip do jsonb, tentativa com falha) passam contra esse banco real.
  - Docker precisou de ajuste de permissão no WSL: usuário não estava no grupo `docker` (`sudo usermod -aG docker marcos` + reiniciar a sessão WSL resolveu — `wsl --shutdown` no Windows ou reiniciar a máquina).

- Download de RPIs do INPI (fase 4):
  - **Calendário oficial do INPI é a fonte de verdade** (`IRpiCalendar`/`InpiRpiCalendar`, Infrastructure): a página `https://revistas.inpi.gov.br/rpi/` lista edição+data real de publicação numa tabela HTML — já reflete o deslocamento de feriado (data real, não a teórica), então resolvemos o problema "RPI muda pro próximo dia útil em feriado" **consultando essa página em vez de calcular feriado por conta própria**. Parse via regex simples sobre `<td>edicao</td><td>data</td>` (a página só lista as ~7 edições mais recentes). `GetMostRecentEditionAsync` (edição sem parâmetro) e `GetPublicationDateAsync(edicao)` (data real de uma edição específica, se estiver na janela recente) retornam `null` em caso de falha de rede/mudança de formato — quem chama cai de volta no `RpiEditionCalculator` (par âncora `RpiSchedule:AnchorEdition`/`AnchorPublicationDate`, hoje `2891`/`2026-06-02`, publicação semanal) como aproximação de última instância. `RpiEditionCalculator` continua existindo só como fallback, não é mais fonte primária.
  - **Endpoint**: `POST /rpis/{tipo}/download/{edicao?}` — se `edicao` vier na rota usa ela (tentando achar a data real no calendário); senão pega a edição mais recente do calendário. Enfileira o trabalho e responde `202 Accepted` na hora (execução em **background**, decisão explícita do usuário desde já nesta fase, não só quando encadearmos as próximas).
  - **Fila de background**: `IBackgroundTaskQueue`/`BackgroundTaskQueue` (`System.Threading.Channels`, padrão oficial "Queued background tasks" do ASP.NET Core) + `QueuedHostedService` (cria um `IServiceScope` novo por job, já que o escopo da request HTTP que enfileirou já terminou quando o job roda). Fila em memória — perde os itens pendentes se o processo reiniciar (aceitável por ora; o histórico de tentativas já concluídas fica no Postgres de qualquer forma).
  - **`RpiFileNaming`** (Domain): mapeia `RpiTipo` → prefixo de arquivo. **Confirmado contra a página real do calendário, não só copiado do legado** — e isso pegou uma divergência real: o legado usava `Programas_de_Computador`, mas a URL real do INPI hoje é `Programa_de_computador` (singular, minúsculo). Os demais batem: `Patentes`, `Marcas`, `Desenhos_Industriais`, `Contratos_de_Tecnologia`, `Indicacoes_Geograficas`, `Comunicados`, e o typo `Topografia_de_circuto_Integrado`.
  - **`InpiRpiDownloader`** (Infrastructure): `HttpClient` nomeado apontando pra `http://revistas.inpi.gov.br/pdf/{arquivo}`, timeout de 300s (`Inpi:HttpTimeoutSeconds`, alto porque roda em background). **Descoberta importante**: o INPI retorna 403 para requests sem `User-Agent` de navegador (WAF bloqueia o UA padrão de HttpClient/curl) — configurado um UA fixo (`Inpi:UserAgent`). O site também redireciona http→https (302) no endpoint de PDF, seguido automaticamente pelo `HttpClient` (`AllowAutoRedirect` é `true` por padrão); o endpoint do calendário já é https direto e não exige UA especial.
  - **Validação do PDF**: checagem leve (não-vazio + assinatura `%PDF` nos primeiros bytes) — suficiente pra esta fase; extração/parsing real de conteúdo fica pras fases 5/7.
  - **Armazenamento local temporário**: `LocalDiskRpiFileStorage` grava em `RpiStorage:LocalWorkingDirectory` — default `../../data/rpi/{edicao}/{arquivo}` **relativo ao content root da Api** (`src/Kodx.Rpi.Api`), o que cai em `data/` na raiz do repositório (não dentro de `src/`), gitignored (`/data/`). Path relativo resolvido contra o `Environment.CurrentDirectory` do processo, que coincide com o content root ao rodar via `dotnet run`/IDE a partir da pasta do projeto — em Docker (fase 6) essa config precisará de um valor absoluto próprio (volume montado), já que não existe hierarquia `src/`/raiz do repo dentro do container. O upload pro Azure Blob Storage é a fase 6; as fases seguintes vão ler desse mesmo diretório.
  - **Testado contra o INPI real** (não só mock): edição âncora 2891/Patentes, edição calculada automaticamente via calendário 2897/Marcas e 2897/`ProgramasComputador` (confirmando a correção do nome do arquivo) baixaram com sucesso, gravando `rpi_processing_attempts` com status `Success` e o arquivo salvo localmente. Uma tentativa de `ProgramasComputador` falhou por instabilidade de rede transitória (`HttpRequestException` ao ler o corpo da resposta) e foi corretamente registrada como `Failure`, sem derrubar a aplicação — repetir a chamada teve sucesso.

- Conversão PDF → TXT (fase 5):
  - **Código do `kodx-pdf` portado (copiado), não referenciado cross-repo** — decisão explícita do usuário. `PdfTextExtractor`/`TextBlock`/`LineGroup` (namespace `Kodx.Rpi.Infrastructure.Pdf`) vieram de `git@github.com:marcosiw/kodx-pdf.git`, com comentário de atribuição no topo do arquivo. Pacote `UglyToad.PdfPig` 1.7.0-custom-5 (confirmado disponível direto no nuget.org, apesar do nome "custom" sugerir build própria).
  - **`ConvertRpiEditionToTxtUseCase`** (Application): lê o PDF local (`IRpiFileStorage.GetPdfPath`), extrai via `IPdfTextExtractor` (portas novas), salva o `.txt` (`IRpiFileStorage.SaveTxtAsync`, usando `RpiFileNaming.TxtFileName`) e grava `RpiProcessingAttempt` com stage `ConvertToTxt`. **Validação de integridade do PDF** (lembrete do usuário): não é um passo separado — o `PdfDocument.Open` do PdfPig (chamado dentro de `ExtractOrdered`) já lança se o arquivo estiver corrompido/ilegível, e isso vira `Failure` como qualquer outra exceção da pipeline. Texto extraído vazio/só espaços também é tratado como falha.
  - **Encadeamento automático** (decisão explícita do usuário): a orquestração "download bem-sucedido → enfileirar conversão" fica no `RpiDownloadController` (não dentro do `DownloadRpiEditionUseCase`, que continua sem saber de fila/background — isso ficaria vazando uma preocupação de Infrastructure/hosting pra dentro da Application). Por isso `DownloadRpiEditionUseCase.ExecuteAsync` passou a retornar `DownloadRpiEditionResult(bool Success, int Edicao)` em vez de `Task` simples — o controller usa esse resultado pra decidir se enfileira `ConvertRpiEditionToTxtUseCase` na mesma fila, já com a edição resolvida (importante quando `edicao` não veio na rota).
  - **Testes portados do `kodx-pdf`**: os testes de unidade de `PdfTextExtractor` (agrupamento de linhas, concatenação de blocos etc.) vieram junto com o código, sem depender de PDFs de amostra (usam `TextBlock`s sintéticos).
  - **Testado contra o INPI real de ponta a ponta**: baixou a edição 2891/Patentes, encadeou a conversão automaticamente, e o texto extraído do PDF real (3.162.451 bytes) bateu exatamente com o cabeçalho oficial da RPI ("Revista da Propriedade Industrial", "Nº 2891", data, nomes de autoridades), sem garble — confirma que a lógica portada do `kodx-pdf` funciona igual fora do repositório original.

- Armazenamento no Azure Blob Storage (fase 6):
  - **Infra real já provisionada** — o usuário já tinha conta de storage e container `rpi` com credenciais, então a fase foi implementada e testada direto contra o Azure real (sem emulador/Azurite no projeto), seguindo o mesmo padrão de rigor das fases 4/5 (INPI real).
  - **`IRpiBlobStorage`** (Application, port nova): `UploadPdfAsync`/`UploadTxtAsync`, recebendo `RpiTipo`/`edicao`/caminho local. **`AzureBlobRpiStorage`** (Infrastructure): usa `Azure.Storage.Blobs` (`BlobServiceClient` singleton, connection string via `BlobStorage__ConnectionString`), sobe o arquivo (`BlobClient.UploadAsync(path, overwrite: true)`) e aplica as 3 tags via `SetTagsAsync` — path e nome de container seguem exatamente a convenção documentada acima.
  - **`UploadRpiEditionToBlobUseCase`** (Application): mesmo formato dos outros use cases da pipeline — lê `RpiEdition` existente, sobe PDF e TXT (usa `IRpiFileStorage.GetPdfPath`/novo `GetTxtPath`), grava `RpiProcessingAttempt` com stage `UploadBlob`.
  - **Encadeamento automático** (mesma decisão de fase 5, estendida): `RpiDownloadController` agora encadeia download → conversão → upload na mesma fila, cada etapa só dispara se a anterior teve sucesso. Por isso `ConvertRpiEditionToTxtUseCase.ExecuteAsync` passou a retornar `Task<bool>` (sucesso), no mesmo espírito do `DownloadRpiEditionResult` da fase 5.
  - **Testado contra o Azure real de ponta a ponta, e um incidente real durante esse teste**: o container `rpi` configurado pelo usuário **já é o container de produção do `kodx-legacy`**, com backups reais de todos os 8 tipos de RPI da edição 2891. O primeiro teste (rpi=`(int)tipo`, ex: `"6"` pra Patentes) sobrescreveu `2891/Patentes2891.pdf`/`.txt` de produção (conteúdo deve ser idêntico — mesmo PDF oficial do INPI — mas a tag `rpi` mudou de formato). Comparando com as tags reais dos outros 7 tipos (todas com o **nome** do tipo, ex: `"Comunicados"`, `"ContratosTecnologia"`), ficou claro que a leitura do código do legado (`rpi.Rpi.ToString()`) tinha levado a uma suposição errada (`RpiTipo` numérico) — em produção o campo é o nome. Corrigido para `tipo.ToString()` (bate exatamente com os nomes do enum `RpiTipo`) e reexecutado o upload de 2891/Patentes pra restaurar a tag correta; confirmado via leitura direta dos blobs/tags após a correção. **Lição**: ao inferir formato de dados a partir só do código-fonte (sem acesso ao banco/storage real), tratar como hipótese a confirmar, não como fato — aqui só foi possível corrigir porque o teste rodou contra a infra real antes do merge.
  - Config: `BlobStorage:ConnectionString` (env `BlobStorage__ConnectionString`, nunca commitado) e `BlobStorage:ContainerName` (default `"rpi"`), mesmo mecanismo único já estabelecido (`.envrc.example`, `launchSettings.json.example`, `appsettings.json`).

- Extração de publicações individuais (fase 7):
  - **Escopo: só os 4 tipos que o legado já processa** (Marcas, Patentes, Desenhos Industriais, Programas de Computador) — decisão explícita do usuário. Os outros 4 tipos (Comunicados, Contratos de Tecnologia, Indicações Geográficas, Topografia de Circuitos) **nunca foram quebrados em publicações em produção** (confirmado explorando o legado: não existe classe `Leitura.Rpi{Tipo}` pra eles) — `ExtractRpiPublicationsUseCase` simplesmente retorna sem registrar tentativa nenhuma pra esses tipos (`IRpiPublicationExtractor.IsSupported`), não é uma falha.
  - **`Rodape`/`Orgao` removidos do `PublicationPayload`** — decisão explícita do usuário. Confirmado no legado que essas duas colunas nunca são preenchidas em lugar nenhum do pipeline de parsing (sempre nulas em produção), então não fazia sentido manter campos mortos no payload novo.
  - **Algoritmo portado (não referenciado) do legado**, arquivos-fonte de referência em `/mnt/e/projects/kodx-legacy/src/4.Dominio/RegrasDeNegocio/`: `Index.cs` (indexação de marcadores no texto) + `ProcessamentoDados.cs` (fatiamento/montagem das publicações) + `Leitura/RpiMarcas.cs`, `RpiPatentes.cs`, `RpiDesenhoIndustrial.cs`, `RpiProgramasdeComputador.cs` (termos de busca por tipo). Não portado: cálculo de dados fixos (Revista/Caderno/Edição/Data — já vêm do `RpiEdition` desta aplicação, não precisam ser reextraídos do texto) nem `IdentificadorTarefa` (código de tarefa de negócio específico de Marcas, fora do escopo do payload desta fase).
  - **Estrutura do código**: motor genérico em `Kodx.Rpi.Infrastructure/Rpis/PublicationExtraction/RpiPublicationExtractionEngine.cs` (índices/fronteiras, herança de cabeçalho, número de processo, página — tudo `internal`), regras por tipo em `RpiMarcasRules.cs`/`RpiPatentesRules.cs`/`RpiDesenhosIndustriaisRules.cs`/`RpiProgramasComputadorRules.cs` (cada um só uma lista de termos literais/regex portados verbatim do legado). `RpiPublicationExtractor` (público, implementa `IRpiPublicationExtractor`) é o único ponto de entrada. `ExtractRpiPublicationsUseCase` (Application) segue o mesmo formato dos demais use cases da pipeline — lê o TXT (`IRpiFileStorage.ReadTxtAsync`, método novo), extrai, substitui publicações antigas da edição via `IPublicationRepository.ReplaceForEditionAsync` (idempotente em reprocessamento) e grava `RpiProcessingAttempt` com stage `ExtractPublications`.
  - **Encadeamento automático** (mesma decisão das fases 5/6): `RpiDownloadController` agora dispara upload e extração **em paralelo lógico** depois da conversão (um não depende do sucesso do outro, só da conversão) — download → conversão → upload → extração, todos na mesma fila.
  - **Descoberta crítica, corrigida**: o `PdfTextExtractor` desta aplicação (fase 5, via PdfPig) gera TXT usando só `\n`, mas todos os termos literais portados do legado (cabeçalhos) usam `\r\n` hardcoded — testando contra o TXT real da edição 2891/Patentes, os cabeçalhos vinham **sempre vazios** apesar do texto batendo visualmente, porque nenhum termo literal batia contra `\n` puro. Corrigido normalizando o texto pra CRLF logo no início de `RpiPublicationExtractionEngine.Extract` (sem alterar os termos portados). As fronteiras de publicação (via regex `[\r\n]...`, que é uma classe de caractere batendo um `\r` OU um `\n`) já funcionavam sem essa normalização — só os termos literais de cabeçalho dependiam dela.
  - **Testes**: `RpiPublicationExtractorTests.cs` (unidade, sem depender de PDFs/TXTs de amostra — usa trechos de texto sintéticos montados com os termos/regex *reais* portados, incluindo casos que expõem peculiaridades fiéis do legado: `Numero` fica com o último padrão de regex que casou, não o primeiro; `TodosNumeros` não deduplica; sobra um `\r` solto em cabeçalhos seguidos de fronteira regex, porque a regex de fronteira só consome o `\n` do `\r\n` anterior — tudo documentado inline nos testes). `ExtractRpiPublicationsUseCaseTests.cs` (integração, Postgres real via docker-compose, extractor fake — cobre tipo não suportado, edição inexistente, sucesso com substituição de publicações antigas, e falha registrada sem exceção não tratada).
  - **Validação cruzada contra o legado (resultado final)**: a extração foi rodada contra PDFs reais do INPI e o resultado (`Numero`, `Cabecalho1/2/3`, `Pagina`) comparado publicação a publicação com uma amostra de dados já processados pelo legado (mesma fonte de dados referenciada na fase 3).
    - **Marcas, Patentes e ProgramasComputador: validados, sem necessidade de mudança de código.** Contagens de publicações e os campos comparados batem de perto contra a amostra do legado.
    - **O "cabeçalho inchado" já visto em Patentes é paridade com o legado, não uma regressão do port**: comparando o conteúdo completo, os casos divergentes têm exatamente o mesmo padrão no próprio dado de origem (parágrafo inteiro em vez de só o título) — ou seja, é assim que o legado já se comporta, não foi introduzido por este port. A hipótese original (descasamento de capitalização entre o termo de cabeçalho esperado e o texto real) é a causa raiz confirmada. **Decisão do usuário: corrigir mesmo assim** (item 12 do plano) — não deixar como dívida permanente só porque o legado tem o mesmo problema; corrigir aqui faz esta aplicação divergir do legado nesses casos (esperado e aceito: a saída nova passa a ser mais correta que a de origem).
    - Sobrou um resíduo pequeno de divergência de `Numero` em Marcas que parece ser um caso raro de uma publicação com múltiplos números onde o motor escolhe um `Numero` primário diferente do legado — não investigado a fundo, aceitável por ora, retomar só se aparecer reclamação real de um serviço consumidor.
    - **DesenhosIndustriais: a extração não encontrou publicações na amostra comparada.** Inspecionando o texto real do PDF, a causa é que o layout que a INPI usa hoje pra esse tipo (headers e formato de número de processo) é diferente do que o parser herdado do legado espera — e essa mudança de layout também afeta a extração no próprio legado, não é uma regressão introduzida aqui. **Decisão do usuário: seguir pro PR só com Marcas/Patentes/ProgramasComputador; DesenhosIndustriais fica de fora por ora** (item 11 do plano) — reescrever `RpiDesenhosIndustriaisRules.cs` do zero a partir do layout atual é um trabalho novo, não um ajuste pontual de termo.

- Endpoints privados via gRPC (fase 8):
  - **Decisão explícita do usuário: gRPC em vez de REST**, migrando também o endpoint de trigger de download que já existia (`RpiDownloadController`) — a API deixou de ter Controllers/MVC, é 100% gRPC agora. Só `/health` continua HTTP puro (infra/ops, não domínio).
  - **Sem Swagger** (a spec pedia, mas gRPC não tem OpenAPI nativo): documentação/exploração via `Grpc.AspNetCore.Server.Reflection` (habilitado em `Development`) + coleção Bruno em `docs/Kodx API/` — confirmado que o Bruno (usebruno.com) suporta gRPC (beta, desde 2.10), incluindo reflection e streaming. `specs/Especificações - Kodx RPI.md` já atualizada com essa decisão.
  - **Serviço único `RpiService`** (`src/Kodx.Rpi.Api/Protos/rpi.proto`, `GrpcServices="Both"` — gera client além do server pra ser usado pelos testes de integração): `TriggerDownload` (unário, migração 1:1 do controller antigo), `GetRpiHistory` (unário, dados da edição + histórico de `RpiProcessingAttempt` — renomeado de `GetRpiEdition`, mais semântico), `DownloadPdf` (server-streaming, lê do Blob Storage — não do disco local, que é temporário), `SearchRpiPublications` (server-streaming — renomeado de `SearchPublications` e com semântica nova, ver abaixo).
  - **`SearchRpiPublications` busca por lote de números de processo, não por edição inteira** — decisão explícita do usuário (validada com discussão sobre lote vs. um-a-um): request recebe `repeated string numeros` + `tipo`/`edicao` **opcionais** (proto3 `optional`, os dois juntos ou nenhum — `InvalidArgument` se só um vier informado). Sem `tipo`/`edicao`, busca os números em todo o histórico já processado; com eles, restringe a uma edição (e valida que ela existe, `NotFound` se não). Escolhido lote (não um número por chamada) porque `numero` já é indexado em `Publication` (decisão da fase 3) — uma lista vira uma única query, independente de terem 5 ou 5000 números; um-a-um custaria N idas ao banco/rede à toa. Como a busca pode abranger várias edições ao mesmo tempo, `PublicationReply` ganhou os campos `tipo`/`edicao` (ausentes antes, quando o escopo era sempre uma edição só).
  - **Tabela nova `publication_numeros` (entidade `PublicationNumero`, migration `AddPublicationNumeros`)** — decisão explícita do usuário. A busca original só batia contra `Publication.Numero` (o número primário, escolhido pela engine de extração como "o último padrão de regex que casou") — números **secundários** de uma publicação (os demais em `Payload.TodosNumeros`, dentro do jsonb, não indexável) nunca eram encontrados. `publication_numeros` é uma tabela relacional 1:N (`PublicationId` FK, `Numero` indexado) com uma linha por número mencionado (primário + secundários, deduplicados — `Publication.Numeros`, calculado no construtor a partir de `Numero`+`Payload.TodosNumeros`). `Payload.TodosNumeros` continua existindo do jeito que sempre foi (fiel ao legado, sem dedup) — a tabela nova é só um índice de busca derivado, não substitui o payload.
    - **`PublicationRepository.ReplaceForEditionAsync` faz save em duas fases**: grava as `Publication` primeiro e chama `SaveChangesAsync` direto (fora do `IUnitOfWork`) pra obter os `Id`s gerados, só depois monta e adiciona as `PublicationNumero` (que dependem desse FK) — mesmo padrão já usado em `DownloadRpiEditionUseCase` pra `RpiEdition`/`RpiProcessingAttempt`. **Pegadinha real encontrada nos testes**: qualquer código que insira uma `Publication` sem passar por esse repositório (ex.: um teste que faz `context.Publications.Add(...)` direto) não popula `publication_numeros` sozinho — o helper de seed de `RpiGrpcServiceTests` precisou ser ajustado pra replicar o mesmo padrão de duas fases.
    - `SearchByNumerosAsync` agora filtra via `dbContext.PublicationNumeros.Any(n => n.PublicationId == p.Id && numeros.Contains(n.Numero))` (subquery `EXISTS`, sem duplicar `Publication` quando mais de um número da lista bate na mesma publicação).
  - **Capacidades de leitura novas** (antes só existia escrita): `IRpiProcessingAttemptRepository.ListForEditionAsync`, `IPublicationRepository.SearchByNumerosAsync` (`IAsyncEnumerable<PublicationSearchResult>`, via `AsAsyncEnumerable()` do EF Core — streaming real do Postgres, sem materializar tudo em memória; `PublicationSearchResult` carrega `Tipo`/`Edicao` de origem junto da `Publication`), `IRpiBlobStorage.DownloadPdfAsync` (via `BlobClient.OpenReadAsync`).
  - **`ApiKeyMiddleware` reaproveitado sem mudança de lógica**: metadata gRPC chega ao pipeline ASP.NET Core como headers HTTP normais, então a checagem de header já existente funciona igual pras chamadas gRPC (confirmado rodando de verdade — um 401 do middleware chega ao cliente gRPC como `StatusCode.Unauthenticated`, mapeamento padrão do protocolo).
  - **Bug real de infra encontrado só ao rodar de verdade (não pelos testes automatizados)**: a suposição inicial de servir gRPC (HTTP/2) e `/health` (HTTP/1.1) na mesma porta, com `Protocols = Http1AndHttp2` sem TLS, **não funciona na prática** — Kestrel aceita a conexão mas nunca completa a negociação (`grpcurl` trava em "context deadline exceeded"; só `curl`/HTTP1.1 puro funcionava). Os testes de integração (`Kodx.Rpi.Api.Tests`) não pegaram isso porque `WebApplicationFactory`/`TestServer` usa transporte em memória, sem passar pela negociação HTTP/2 real do Kestrel. **Corrigido** com duas portas dedicadas via `Kestrel:Endpoints` em `appsettings.json`: `Grpc` (8080, `Protocols: Http2`) e `Health` (8081, `Protocols: Http1`) — `Dockerfile` (`EXPOSE 8080 8081`) e `.envrc.example` atualizados. **Lição, no mesmo espírito da fase 6**: verificação real (rodar a aplicação de verdade e bater com uma ferramenta real, aqui `grpcurl`) pegou um problema que testes com transporte in-memory não conseguem pegar — vale a pena sempre que a camada de transporte/protocolo for parte do que está sendo validado.
  - **Testes**: `Kodx.Rpi.Api.Tests` (novo projeto) — `RpiGrpcServiceTests.cs`, integração via `WebApplicationFactory<Program>` + `Grpc.Net.Client` (canal real sobre o `TestServer`), cobrindo as 4 RPCs (sucesso, `NotFound`, streaming com múltiplos itens/chunks, rejeição sem API key). Fila de background e Blob Storage são fakes (`FakeBackgroundTaskQueue`/`FakeRpiBlobStorage`) — evita disparar downloads reais do INPI/Azure durante os testes; Postgres real via docker-compose, mesmo padrão dos outros testes de integração do projeto.
  - **Verificação manual de ponta a ponta**: rodada com `grpcurl` de verdade (reflection, as 4 RPCs, incluindo os casos de `NotFound`) e confirmado que o job de `TriggerDownload` em background continua funcionando igual (mesma pipeline download → conversão → upload/extração, reaproveitada sem mudança de lógica).

## Perguntas em aberto

- A infraestrutura real do Postgres em produção (Linode) já está provisionada e com credenciais disponíveis para deploy, ou seguimos só com o Postgres local via docker-compose por enquanto? (Blob Storage no Azure já confirmado — ver fase 6 acima)
